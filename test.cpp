#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;
#define vi vector<int>
#define vii vector<vi>
#define rep(i,a,b) for(i=a;i<b;++i)

int main(){
    int i,j,k;
    int n,m;
    scanf("%d%d",&n,&m);
    int arr[n+1][m+1];
    rep(i,0,n)
        rep(j,0,m)
            scanf("%d",&arr[i][j]);

    for(int i=0;i<n;++i){
        
    }
}